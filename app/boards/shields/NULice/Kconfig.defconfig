if NULICE_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Nulice"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if NULICE_LEFT || NULICE_RIGHT

config ZMK_SPLIT
    default y

endif